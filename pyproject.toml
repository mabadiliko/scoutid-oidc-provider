[project]
name = "scoutid-oidc-provider"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "httpx",
    "itsdangerous",
    "jinja2",
    "joserfc",
    "python-multipart",
    "uvicorn[standard]",
]
